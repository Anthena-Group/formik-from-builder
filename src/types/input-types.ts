export enum InputTypes {
    TEXT = "text",
    MULTI_TEXT = "multi-text",
    CHECKBOX = "checkbox",
    SELECT = "select",
    AUTO_COMPLETE="autocomplete",
    RADIO = "radio",
    DROPDOWN = "dropdown",
    DATE = "date",
}